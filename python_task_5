# Task 5: Count strings with length >= 2 and same first and last characters
def lst(a):
    count = 0
    for i in a:
        if len(i) >= 2 and i[0] == i[-1]:
            count += 1
    return count

print(lst(["abc", "121", "nun", "030"]))
