# Task 35:
def new(a, n):
    lst = []
    for i in range(1, n+1):
        for j in a:
            lst.append(f"{j}{i}")
    return lst

usr_input = int(input("Give a number : "))
print(new(["y","z"], usr_input))
